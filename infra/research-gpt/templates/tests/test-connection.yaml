apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "research-gpt.fullname" . }}-test-connection"
  labels:
    {{- include "research-gpt.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "research-gpt.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
